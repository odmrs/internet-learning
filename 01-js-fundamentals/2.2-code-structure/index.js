// Semicollons after statments can help us to avoid erros
alert("Hiii");
alert("Hiii!!"); // This is possible because the semicolon tell for the browser that line is break

// But omit this semmicolons can generate errors: tell the alert is undefined
alert("Hello");
[(1, 2)].forEach(alert);

// Comments one line 

/* 
 * comments of multiply lines
*/
